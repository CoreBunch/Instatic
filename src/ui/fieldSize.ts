/**
 * One control-size scale for every field primitive (Input, Select,
 * ColorInput, …). The tiers map to the shared height tokens in globals.css:
 * xs → --control-height-sm (24px), sm → --control-height (30px),
 * md → --control-height-lg (36px).
 */
export type FieldSize = 'xs' | 'sm' | 'md'
